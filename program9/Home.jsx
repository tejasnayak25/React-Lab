import React from "react";

function Home() {
    return (
        <div style={{display: "flex", flexDirection: "column", justifyContent: "center", alignItems: "center"}}>
            <h2>Welcome to React Router DOM Example</h2>
            <p>This is a simple project demonstrating routing using React Router DOM</p>
            <p>Use the navigation bar to visit other pages.</p>
        </div>
    );
}

export default Home;